"""Schemas Pydantic — Validación de requests y responses de la API.

Cada schema valida datos que entran (request) y formatea datos que salen (response).
Regla de oro: NUNCA exponer modelos SQLAlchemy directamente en la API.
"""
